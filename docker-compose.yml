services:
  app:
    build: .
    container_name: zotero-mcp-app
    restart: unless-stopped
    ports:
      - "127.0.0.1:8100:8100"
    environment:
      - PORT=8100
      - MCP_DATA_DIR=/app/data
    env_file:
      - .env
    volumes:
      - ./data:/app/data
